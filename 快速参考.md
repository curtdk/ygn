# 快速参考

## 🚀 快速开始

```bash
# 1. 部署后端
cd /www/wwwroot/nextjs/ygn
./deploy-video-generation.sh

# 2. 启动Medusa
cd medushaV2
npm run dev

# 3. 访问Admin
# http://localhost:9000/app
```

## 📁 关键文件位置

### 后端模块
```
medushaV2/src/modules/
├── video-material/          # 素材管理模块
│   ├── models/video-material.ts
│   ├── service.ts
│   └── index.ts
└── user-video/              # 用户视频模块
    ├── models/user-video.ts
    ├── service.ts
    └── index.ts
```

### API路由
```
medushaV2/src/api/
├── store/                   # 前端API
│   ├── products/[id]/materials/route.ts
│   ├── video-generation/route.ts
│   └── my-videos/route.ts
└── admin/                   # 后台API
    └── video-materials/
        ├── route.ts
        └── [id]/route.ts
```

### Admin UI
```
medushaV2/src/admin/widgets/
└── video-materials-widget.tsx
```

## 🔌 API端点

### Store API
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/store/products/{id}/materials` | 获取产品素材 |
| POST | `/store/video-generation` | 创建视频任务 |
| GET | `/store/my-videos` | 获取用户视频 |

### Admin API
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/admin/video-materials` | 获取素材列表 |
| POST | `/admin/video-materials` | 创建素材 |
| POST | `/admin/video-materials/{id}` | 更新素材 |
| DELETE | `/admin/video-materials/{id}` | 删除素材 |

## 📊 数据库表

### video_material
```sql
id, product_id, name, material_key,
material_type, default_url, is_replaceable, sort_order
```

### user_video
```sql
id, user_id, order_id, product_id, video_url,
thumbnail_url, title, duration, status,
materials_used, error_message
```

## 💻 前端集成

### 获取素材
```typescript
const { materials } = useProductMaterials(productId)
```

### 上传文件
```typescript
const result = await uploadMaterial(file)
```

### 创建视频
```typescript
await createVideoGeneration({
  product_id: "prod_01",
  title: "我的视频",
  materials_used: {...}
})
```

### 获取视频列表
```typescript
const { videos } = useMyVideos()
```

## 🔧 常用命令

```bash
# 构建项目
npm run build

# 生成迁移
npx medusa db:generate

# 运行迁移
npx medusa db:migrate

# 启动开发服务器
npm run dev

# 回滚迁移
npx medusa db:migrate:down
```

## ⚠️ 待实现功能

- [ ] 积分系统集成
- [ ] 订单系统集成
- [ ] 视频生成服务
- [ ] 文件存储配置
- [ ] 通知系统
- [ ] 前端页面修改

## 📚 文档索引

1. **视频生成业务实现方案.md** - 完整技术方案
2. **视频生成功能使用指南.md** - 使用和部署指南
3. **数据库迁移说明.md** - 数据库操作
4. **前端API集成示例.tsx** - React代码示例
5. **实施总结.md** - 项目总结
6. **快速参考.md** - 本文档

## 🐛 常见问题

**Q: 模块未找到？**
A: 检查 `medusa-config.ts` 并重新构建

**Q: 表不存在？**
A: 运行 `npx medusa db:migrate`

**Q: API 404？**
A: 检查路由文件路径和文件名

**Q: Widget不显示？**
A: 重新构建Admin并刷新页面

## 📞 获取帮助

查看详细文档：
- 技术方案：`视频生成业务实现方案.md`
- 使用指南：`视频生成功能使用指南.md`
- 代码示例：`前端API集成示例.tsx`
