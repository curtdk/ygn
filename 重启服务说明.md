# 🔄 需要重启服务使新密钥生效

## ✅ 配置已完成

所有配置文件已经更新为使用统一的强密钥：

```
密钥: aaqduqnkBiHEprjcqzr-KEfbsOoSH_5SNnY04gm-uBs
```

**配置位置**:
- ✅ `scripts/start-meilisearch.sh` - 已更新
- ✅ `medushaV2/.env` - 已更新
- ✅ `medushaV2/.env.template` - 已添加说明

---

## ⚠️ 重要：需要重启服务

由于密钥配置已更新，需要重启 Meilisearch 服务使新密钥生效。

### 当前状态

运行验证脚本查看状态：
```bash
./scripts/verify-meilisearch-key.sh
```

如果看到 "❌ 使用脚本密钥失败"，说明需要重启。

---

## 🚀 重启步骤

### 步骤 1: 停止当前 Meilisearch 服务

找到运行 Meilisearch 的终端窗口，按 `Ctrl+C` 停止服务。

**或者使用命令停止**:
```bash
pkill -f meilisearch
```

### 步骤 2: 清除旧数据（可选但推荐）

为了确保使用新密钥，建议清除旧的 Meilisearch 数据：

```bash
rm -rf meilisearch_data/
```

> **注意**: 这会删除所有索引数据，但产品会在 Medusa 启动后自动重新索引。

### 步骤 3: 使用新密钥启动 Meilisearch

```bash
./scripts/start-meilisearch.sh
```

您应该看到：
```
========================================
启动 Meilisearch v1.6
========================================
配置信息:
  - 地址: http://localhost:7700
  - 主密钥: aaqduqnkBiHEprjcqzr-KEfbsOoSH_5SNnY04gm-uBs
  - 环境: development
  - 数据目录: ./meilisearch_data
========================================
```

**保持此终端窗口运行**

### 步骤 4: 重启 Medusa 后端（如果正在运行）

找到运行 Medusa 的终端窗口，按 `Ctrl+C` 停止，然后重新启动：

```bash
cd medushaV2
npm run dev
```

### 步骤 5: 验证配置

运行验证脚本：
```bash
./scripts/verify-meilisearch-key.sh
```

应该看到 "✅ 使用脚本密钥成功"

运行完整测试：
```bash
./scripts/test-search-setup.sh
```

所有测试应该通过。

---

## 📋 完整启动命令（3 个终端）

### 终端 1 - Meilisearch
```bash
./scripts/start-meilisearch.sh
```

### 终端 2 - Medusa 后端
```bash
cd medushaV2
npm run dev
```

### 终端 3 - 前端（如果需要）
```bash
cd medushaV2-storefront
yarn dev
```

---

## ✅ 验证清单

完成重启后，请检查：

- [ ] Meilisearch 服务正常运行
- [ ] 验证脚本显示密钥一致
- [ ] 验证脚本显示使用脚本密钥成功
- [ ] Medusa 后端正常启动
- [ ] 测试脚本 8/8 测试通过
- [ ] 前端搜索功能正常工作

---

## 🐛 故障排查

### 问题 1: 验证脚本仍然失败

**解决方案**:
1. 确认 Meilisearch 已完全停止：`pkill -f meilisearch`
2. 删除旧数据：`rm -rf meilisearch_data/`
3. 重新启动：`./scripts/start-meilisearch.sh`

### 问题 2: Medusa 后端报错

**错误**: "Meilisearch connection failed"

**解决方案**:
1. 确认 Meilisearch 正在运行：`curl http://localhost:7700/health`
2. 检查密钥是否一致：`./scripts/verify-meilisearch-key.sh`
3. 重启 Medusa 后端

### 问题 3: 搜索无结果

**解决方案**:
1. 在 Medusa Admin 中编辑并保存一个产品（触发索引）
2. 等待几秒钟让索引完成
3. 测试搜索功能

---

## 📚 相关文档

- [MEILISEARCH_API_KEY_说明.md](./MEILISEARCH_API_KEY_说明.md) - 密钥配置详解
- [SEARCH_QUICK_START.md](./SEARCH_QUICK_START.md) - 快速启动指南
- [搜索功能验证报告.md](./搜索功能验证报告.md) - 测试报告

---

## 💡 提示

**为什么需要重启？**

Meilisearch 在启动时读取主密钥（`MEILI_MASTER_KEY`），之后无法动态更改。因此修改密钥后必须重启服务。

**数据会丢失吗？**

- 如果不删除 `meilisearch_data/` 目录，索引数据会保留
- 但由于密钥变化，建议清除旧数据重新索引
- Medusa 会自动重新索引所有产品

---

**准备好了吗？** 开始重启服务吧！

按照上述步骤操作后，运行 `./scripts/test-search-setup.sh` 验证一切正常。
