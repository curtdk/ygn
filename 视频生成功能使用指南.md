# 视频生成功能使用指南

## 后端部署步骤

### 1. 运行数据库迁移

```bash
cd medushaV2
npm run build
npx medusa db:migrate
```

这将创建以下数据表：
- `video_material`: 存储产品的可替换素材配置
- `user_video`: 存储用户生成的视频记录

### 2. 启动Medusa服务

```bash
npm run dev
```

## 后台管理使用

### 1. 创建视频生成产品

1. 登录Medusa Admin（默认：http://localhost:9000/app）
2. 进入 Products 页面
3. 创建新产品或编辑现有产品
4. 在产品详情页面，找到"视频可替换素材"部分
5. 点击"添加素材"按钮

### 2. 配置可替换素材

为每个素材配置以下信息：
- **素材名称**: 显示给用户的名称（如：角色1、背景音乐）
- **素材标识**: 用于程序识别的key（如：character_1、bg_music）
- **类型**: 选择素材类型（图片/声音/背景/视频）
- **默认素材URL**: 默认使用的素材地址
- **可替换**: 是否允许用户替换

示例配置：
```
名称: 角色1
标识: character_1
类型: 图片
默认URL: /assets/default-character.jpg
可替换: 是
```

### 3. 在产品metadata中标记类型

在产品的metadata字段中添加：
```json
{
  "product_type": "video_generation",
  "video_config": {
    "template_id": "template_001",
    "duration": 45
  }
}
```

## API端点

### Store API（前端调用）

#### 1. 获取产品的可替换素材
```
GET /store/products/{product_id}/materials
```

响应示例：
```json
{
  "materials": [
    {
      "id": "mat_01",
      "product_id": "prod_01",
      "name": "角色1",
      "material_key": "character_1",
      "material_type": "image",
      "default_url": "/assets/sister.pic",
      "is_replaceable": true,
      "sort_order": 1
    }
  ]
}
```

#### 2. 创建视频生成任务
```
POST /store/video-generation
```

请求体：
```json
{
  "product_id": "prod_01",
  "title": "温馨家庭聚餐",
  "materials_used": {
    "character_1": {
      "original_url": "/assets/sister.pic",
      "replaced_url": "/uploads/user_123/photo_1.jpg",
      "type": "image"
    },
    "bg_music": {
      "original_url": "/assets/music.mp3",
      "replaced_url": null,
      "type": "audio"
    }
  }
}
```

响应：
```json
{
  "video": {
    "id": "uv_01",
    "status": "pending",
    "title": "温馨家庭聚餐"
  },
  "message": "视频生成任务已创建"
}
```

#### 3. 获取用户的视频列表
```
GET /store/my-videos
```

响应示例：
```json
{
  "videos": [
    {
      "id": "uv_01",
      "title": "温馨家庭聚餐",
      "video_url": "https://cdn.example.com/videos/uv_01.mp4",
      "thumbnail_url": "https://cdn.example.com/thumbnails/uv_01.jpg",
      "duration": 45,
      "status": "completed",
      "created_at": "2024-01-20T10:00:00Z"
    }
  ]
}
```

### Admin API（后台管理）

#### 1. 获取素材列表
```
GET /admin/video-materials?product_id={product_id}
```

#### 2. 创建素材
```
POST /admin/video-materials
```

#### 3. 更新素材
```
POST /admin/video-materials/{id}
```

#### 4. 删除素材
```
DELETE /admin/video-materials/{id}
```

## 前端集成

### 1. 在Home页面获取视频生成产品

```typescript
// react/src/pages/Home.tsx
const [videoProducts, setVideoProducts] = useState([])

useEffect(() => {
  fetch('/api/store/products?metadata[product_type]=video_generation')
    .then(res => res.json())
    .then(data => setVideoProducts(data.products))
}, [])
```

### 2. 在ConfigureRole页面显示可替换素材

```typescript
// react/src/pages/ConfigureRole.tsx
const [materials, setMaterials] = useState([])

useEffect(() => {
  if (productId) {
    fetch(`/api/store/products/${productId}/materials`)
      .then(res => res.json())
      .then(data => setMaterials(data.materials))
  }
}, [productId])
```

### 3. 处理素材替换

```typescript
const handleMaterialReplace = async (materialKey, file) => {
  const formData = new FormData()
  formData.append('file', file)

  const response = await fetch('/api/store/uploads', {
    method: 'POST',
    body: formData
  })

  const data = await response.json()

  setSelectedMaterials(prev => ({
    ...prev,
    [materialKey]: {
      url: data.url,
      type: materials.find(m => m.material_key === materialKey).material_type
    }
  }))
}
```

### 4. 提交视频生成请求

```typescript
const handleGenerate = async () => {
  const response = await fetch('/api/store/video-generation', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      product_id: productId,
      materials_used: selectedMaterials,
      title: productTitle
    })
  })

  const data = await response.json()
  navigate(`/generate?videoId=${data.video.id}`)
}
```

## 待实现功能

以下功能需要根据实际业务需求进一步实现：

### 1. 积分系统集成
- 检查用户积分余额
- 扣除视频生成所需积分
- 记录积分交易

### 2. 订单系统集成
- 创建视频生成订单
- 关联产品和价格
- 订单状态管理

### 3. 视频生成服务
- 集成AI视频生成API（如Runway ML、D-ID等）
- 处理素材上传和转换
- 视频渲染和合成
- 生成缩略图

### 4. 文件存储
- 配置OSS服务（阿里云OSS、AWS S3等）
- 实现文件上传接口
- CDN加速配置

### 5. 通知系统
- 视频生成完成通知
- 生成失败通知
- 邮件/短信/推送通知

### 6. 工作流完善
- 实现generateVideoWorkflow的具体逻辑
- 添加错误处理和重试机制
- 添加任务队列（如Bull、BullMQ）

## 测试

### 1. 测试素材管理
```bash
# 创建素材
curl -X POST http://localhost:9000/admin/video-materials \
  -H "Content-Type: application/json" \
  -d '{
    "product_id": "prod_01",
    "name": "角色1",
    "material_key": "character_1",
    "material_type": "image",
    "default_url": "/assets/sister.pic",
    "is_replaceable": true,
    "sort_order": 1
  }'

# 获取素材列表
curl http://localhost:9000/admin/video-materials?product_id=prod_01
```

### 2. 测试视频生成
```bash
# 创建视频生成任务
curl -X POST http://localhost:9000/store/video-generation \
  -H "Content-Type: application/json" \
  -d '{
    "product_id": "prod_01",
    "title": "测试视频",
    "materials_used": {
      "character_1": {
        "original_url": "/assets/sister.pic",
        "replaced_url": "/uploads/test.jpg",
        "type": "image"
      }
    }
  }'

# 获取用户视频列表
curl http://localhost:9000/store/my-videos
```

## 故障排查

### 问题1: 模块未注册
错误: `Cannot resolve module "videoMaterialModuleService"`

解决: 确保在 `medusa-config.ts` 中正确注册了模块，并重启服务

### 问题2: 数据库表不存在
错误: `relation "video_material" does not exist`

解决: 运行数据库迁移 `npx medusa db:migrate`

### 问题3: API路由404
错误: `Cannot GET /store/products/xxx/materials`

解决: 检查路由文件路径是否正确，确保文件名为 `route.ts`

## 下一步

1. 实现积分系统和订单系统集成
2. 集成实际的视频生成服务
3. 配置文件存储和CDN
4. 完善错误处理和日志记录
5. 添加单元测试和集成测试
6. 优化性能和用户体验
