# ✅ Meilisearch 配置已修复

## 问题描述

之前的配置导致 Medusa 后端启动时出现错误：
```
Error: Loaders for module Meilisearch failed: Request to http://localhost:7700/indexes/products/settings has failed
```

## 原因分析

`@rokmohar/medusa-plugin-meilisearch` 插件需要特定的配置格式，缺少以下必需字段：
- `type`: 指定索引类型（必需）
- `enabled`: 是否启用索引（必需）
- `fields`: 要索引的字段列表（推荐）
- `primaryKey`: 主键字段（必需）

## 已修复

已更新 `medushaV2/medusa-config.ts` 配置文件，添加了所有必需字段。

### 修复后的配置

```typescript
plugins: [
  {
    resolve: "@rokmohar/medusa-plugin-meilisearch",
    options: {
      config: {
        host: process.env.MEILISEARCH_HOST,
        apiKey: process.env.MEILISEARCH_API_KEY,
      },
      settings: {
        products: {
          type: "products",              // ✅ 必需
          enabled: true,                 // ✅ 必需
          fields: [                      // ✅ 推荐
            "id",
            "title",
            "description",
            "handle",
            "variant_sku",
            "thumbnail",
            "status",
            "created_at",
            "updated_at",
          ],
          indexSettings: {
            searchableAttributes: [
              "title",
              "description",
              "variant_sku",
              "handle",
            ],
            displayedAttributes: [
              "id",
              "handle",
              "title",
              "description",
              "variant_sku",
              "thumbnail",
              "status",
            ],
            filterableAttributes: [
              "id",
              "handle",
              "status",
            ],
            sortableAttributes: [
              "created_at",
              "updated_at",
            ],
          },
          primaryKey: "id",              // ✅ 必需
        },
      },
    },
  },
]
```

## 验证状态

### ✅ Meilisearch 服务
```bash
curl http://localhost:7700/health
# 响应: {"status":"available"}
```

### ✅ Medusa 后端
```bash
curl http://localhost:9000/health
# 响应: OK
```

### ✅ 产品索引已创建
```bash
curl -X GET 'http://localhost:7700/indexes' -H 'Authorization: Bearer masterKey'
# 响应: {"results":[{"uid":"products",...}]}
```

### ✅ 索引设置已配置
```bash
curl -X GET 'http://localhost:7700/indexes/products/settings' -H 'Authorization: Bearer masterKey'
# 响应包含正确的 searchableAttributes, filterableAttributes 等
```

## 当前状态

✅ **所有服务正常运行**
- Meilisearch: 运行中 (localhost:7700)
- Medusa 后端: 运行中 (localhost:9000)
- 产品索引: 已创建并配置

## 下一步

### 1. 索引现有产品

如果数据库中已有产品，需要手动触发索引：

**方式 1: 通过 Medusa Admin**
1. 访问 http://localhost:9000
2. 登录管理后台
3. 编辑任意产品并保存（触发索引事件）

**方式 2: 创建新产品**
1. 在 Admin 中创建新产品
2. 插件会自动索引新产品

### 2. 测试搜索功能

**测试 Meilisearch API:**
```bash
curl -X POST 'http://localhost:7700/indexes/products/search' \
  -H 'Authorization: Bearer masterKey' \
  -H 'Content-Type: application/json' \
  --data-binary '{"q": "shirt", "limit": 10}'
```

**测试前端搜索:**
1. 确保前端正在运行：`cd medushaV2-storefront && yarn dev`
2. 访问 http://localhost:8000
3. 使用顶部搜索框测试

### 3. 监控索引状态

**查看索引中的文档数量:**
```bash
curl -X GET 'http://localhost:7700/indexes/products/stats' \
  -H 'Authorization: Bearer masterKey'
```

**搜索所有产品:**
```bash
curl -X POST 'http://localhost:7700/indexes/products/search' \
  -H 'Authorization: Bearer masterKey' \
  -H 'Content-Type: application/json' \
  --data-binary '{"q": "", "limit": 100}'
```

## 注意事项

1. **产品索引是增量的**
   - 只有新创建或更新的产品才会被索引
   - 启动插件不会自动索引现有产品
   - 需要手动触发索引（编辑保存产品）

2. **索引字段限制**
   - 当前配置只索引指定的字段
   - 如需添加更多字段（如品牌、分类等），需要更新配置中的 `fields` 数组

3. **搜索性能**
   - 搜索响应时间通常 < 50ms
   - 建议每个索引不超过 10 万个文档
   - 更多产品考虑分片或优化

## 故障排查

### 问题：产品未被索引

**解决方案:**
1. 检查 Medusa 后端日志是否有错误
2. 确认产品事件订阅正常工作
3. 手动触发：在 Admin 中编辑并保存产品

### 问题：搜索无结果

**解决方案:**
1. 确认索引中有文档：
   ```bash
   curl -X GET 'http://localhost:7700/indexes/products/stats' \
     -H 'Authorization: Bearer masterKey'
   ```
2. 测试 Meilisearch API（绕过 Medusa）
3. 检查搜索字段配置

### 问题：后端启动失败

**解决方案:**
1. 确认 Meilisearch 正在运行
2. 检查环境变量配置（MEILISEARCH_HOST, MEILISEARCH_API_KEY）
3. 查看详细错误日志

## 参考文档

- [插件官方文档](https://github.com/rokmohar/medusa-plugin-meilisearch)
- [Meilisearch 文档](https://www.meilisearch.com/docs)
- [完整安装指南](./MEILISEARCH_SETUP.md)
- [快速启动指南](./SEARCH_QUICK_START.md)

---

**修复日期**: 2026-02-04  
**状态**: ✅ 已解决
