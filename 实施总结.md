# 忆时光视频生成业务 - 实施总结

## 已完成的工作

### 1. 数据库模块 ✅

创建了两个新的Medusa模块：

#### video-material 模块
- **位置**: `medushaV2/src/modules/video-material/`
- **功能**: 管理产品的可替换素材配置
- **文件**:
  - `models/video-material.ts` - 数据模型定义
  - `service.ts` - 服务层
  - `index.ts` - 模块导出

#### user-video 模块
- **位置**: `medushaV2/src/modules/user-video/`
- **功能**: 管理用户生成的视频记录
- **文件**:
  - `models/user-video.ts` - 数据模型定义
  - `service.ts` - 服务层
  - `index.ts` - 模块导出

### 2. API路由 ✅

#### Store API（前端使用）
- `GET /store/products/[id]/materials` - 获取产品的可替换素材
- `POST /store/video-generation` - 创建视频生成任务
- `GET /store/my-videos` - 获取用户的视频列表

#### Admin API（后台管理）
- `GET /admin/video-materials` - 获取素材列表
- `POST /admin/video-materials` - 创建素材
- `POST /admin/video-materials/[id]` - 更新素材
- `DELETE /admin/video-materials/[id]` - 删除素材

### 3. 后台管理UI ✅

创建了Admin Widget组件：
- **位置**: `medushaV2/src/admin/widgets/video-materials-widget.tsx`
- **功能**: 在产品详情页面管理视频素材
- **特性**:
  - 显示素材列表
  - 添加新素材
  - 删除素材
  - 支持多种素材类型（图片、声音、背景、视频）

### 4. 工作流框架 ✅

创建了视频生成工作流：
- **位置**: `medushaV2/src/workflows/generate-video/index.ts`
- **状态**: 框架已创建，具体逻辑待实现

### 5. 配置文件 ✅

更新了Medusa配置：
- **文件**: `medushaV2/medusa-config.ts`
- **变更**: 注册了新的模块

### 6. 文档 ✅

创建了完整的文档：
- `视频生成业务实现方案.md` - 详细的技术方案
- `视频生成功能使用指南.md` - 使用和部署指南
- `数据库迁移说明.md` - 数据库迁移步骤
- `前端API集成示例.tsx` - React集成示例代码

### 7. 部署脚本 ✅

创建了快速部署脚本：
- **文件**: `deploy-video-generation.sh`
- **功能**: 自动化构建、迁移和部署流程

## 数据库结构

### video_material 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 主键 |
| product_id | string | 产品ID |
| name | string | 素材名称 |
| material_key | string | 素材标识 |
| material_type | enum | 类型（image/audio/background/video） |
| default_url | string | 默认素材URL |
| is_replaceable | boolean | 是否可替换 |
| sort_order | number | 排序 |

### user_video 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 主键 |
| user_id | string | 用户ID |
| order_id | string | 订单ID |
| product_id | string | 产品ID |
| video_url | string | 视频URL |
| thumbnail_url | string | 缩略图URL |
| title | string | 标题 |
| duration | number | 时长（秒） |
| status | enum | 状态（pending/processing/completed/failed） |
| materials_used | json | 使用的素材 |
| error_message | string | 错误信息 |

## 业务流程

### 1. 后台配置流程
```
1. 管理员登录Medusa Admin
2. 创建或编辑产品
3. 在产品详情页配置视频素材
4. 设置素材名称、类型、默认URL等
5. 保存配置
```

### 2. 用户生成视频流程
```
1. 用户在前端选择视频生成产品
2. 系统显示可替换的素材
3. 用户选择是否替换素材（上传自己的照片等）
4. 用户点击"生成视频"
5. 系统扣除积分，创建订单
6. 创建视频生成记录（状态：pending）
7. 触发视频生成工作流
8. 更新状态为processing
9. 调用AI服务生成视频
10. 上传视频到存储服务
11. 更新状态为completed，保存视频URL
12. 用户在"我的回忆"中查看视频
```

## 下一步需要实现的功能

### 高优先级 🔴

1. **积分系统集成**
   - 检查用户积分余额
   - 扣除视频生成所需积分
   - 记录积分交易历史

2. **订单系统集成**
   - 创建视频生成订单
   - 关联产品和价格
   - 订单状态同步

3. **视频生成服务**
   - 选择并集成AI视频生成服务（Runway ML、D-ID、HeyGen等）
   - 实现素材处理和转换
   - 实现视频渲染逻辑

4. **文件存储**
   - 配置OSS服务（阿里云OSS或AWS S3）
   - 实现文件上传API
   - 配置CDN加速

### 中优先级 🟡

5. **工作流完善**
   - 完善generateVideoWorkflow的具体实现
   - 添加错误处理和重试机制
   - 集成任务队列（Bull/BullMQ）

6. **通知系统**
   - 视频生成完成通知
   - 生成失败通知
   - 支持多种通知方式（站内信、邮件、推送）

7. **前端页面修改**
   - 修改Home.tsx，从API获取产品
   - 修改ConfigureRole.tsx，集成素材替换
   - 修改GenerateVideo.tsx，显示生成进度
   - 修改Memories.tsx，从API获取视频列表

### 低优先级 🟢

8. **性能优化**
   - 视频生成队列优化
   - 缓存策略
   - 数据库查询优化

9. **监控和日志**
   - 添加详细的日志记录
   - 监控视频生成成功率
   - 性能指标收集

10. **测试**
    - 单元测试
    - 集成测试
    - 端到端测试

## 部署步骤

### 1. 准备环境

确保已安装：
- Node.js 18+
- PostgreSQL 或 MySQL
- npm 或 yarn

### 2. 运行部署脚本

```bash
cd /www/wwwroot/nextjs/ygn
chmod +x deploy-video-generation.sh
./deploy-video-generation.sh
```

### 3. 手动步骤（如果脚本失败）

```bash
# 进入Medusa目录
cd medushaV2

# 安装依赖（如果需要）
npm install

# 构建项目
npm run build

# 生成迁移
npx medusa db:generate

# 运行迁移
npx medusa db:migrate

# 启动服务
npm run dev
```

### 4. 验证部署

1. 访问 http://localhost:9000/app
2. 登录Admin
3. 创建测试产品
4. 在产品详情页查看"视频可替换素材"部分
5. 添加测试素材
6. 使用Postman或curl测试API

## API测试示例

### 测试素材管理

```bash
# 创建素材
curl -X POST http://localhost:9000/admin/video-materials \
  -H "Content-Type: application/json" \
  -d '{
    "product_id": "prod_01",
    "name": "角色1",
    "material_key": "character_1",
    "material_type": "image",
    "default_url": "/assets/sister.pic",
    "is_replaceable": true,
    "sort_order": 1
  }'

# 获取素材列表
curl http://localhost:9000/store/products/prod_01/materials
```

### 测试视频生成

```bash
# 创建视频生成任务
curl -X POST http://localhost:9000/store/video-generation \
  -H "Content-Type: application/json" \
  -d '{
    "product_id": "prod_01",
    "title": "测试视频",
    "materials_used": {
      "character_1": {
        "original_url": "/assets/sister.pic",
        "replaced_url": "/uploads/test.jpg",
        "type": "image"
      }
    }
  }'

# 获取用户视频列表
curl http://localhost:9000/store/my-videos
```

## 技术栈

- **后端框架**: Medusa v2
- **数据库**: PostgreSQL/MySQL
- **ORM**: MikroORM
- **前端**: React + TypeScript
- **UI组件**: Medusa UI
- **文件存储**: 待配置（阿里云OSS/AWS S3）
- **视频生成**: 待集成（Runway ML/D-ID/HeyGen）

## 注意事项

1. **数据库连接**: 确保 `.env` 文件中的 `DATABASE_URL` 配置正确
2. **模块注册**: 新模块必须在 `medusa-config.ts` 中注册
3. **迁移顺序**: 必须先构建项目，再生成和运行迁移
4. **API认证**: Store API需要用户认证，Admin API需要管理员认证
5. **文件上传**: 需要配置文件上传中间件和存储服务
6. **视频生成**: 需要集成第三方AI服务或自建服务

## 故障排查

### 问题1: 模块未找到
**错误**: `Cannot resolve module "videoMaterialModuleService"`

**解决**:
1. 检查 `medusa-config.ts` 中是否正确注册模块
2. 重新构建项目: `npm run build`
3. 重启服务

### 问题2: 数据库表不存在
**错误**: `relation "video_material" does not exist`

**解决**:
1. 运行迁移: `npx medusa db:migrate`
2. 如果迁移失败，查看 `数据库迁移说明.md` 手动创建表

### 问题3: API 404错误
**错误**: `Cannot GET /store/products/xxx/materials`

**解决**:
1. 检查路由文件路径是否正确
2. 确保文件名为 `route.ts`
3. 重新构建项目

### 问题4: Widget不显示
**错误**: Admin页面看不到视频素材Widget

**解决**:
1. 检查Widget文件是否在 `src/admin/widgets/` 目录
2. 确保导出了 `config` 对象
3. 重新构建Admin: `npm run build`

## 联系和支持

如有问题，请查看：
- `视频生成功能使用指南.md` - 详细使用说明
- `视频生成业务实现方案.md` - 技术方案
- `前端API集成示例.tsx` - 前端集成代码

## 更新日志

### 2024-02-12
- ✅ 创建video-material和user-video模块
- ✅ 实现Store和Admin API路由
- ✅ 创建Admin Widget组件
- ✅ 编写完整文档
- ✅ 创建部署脚本
- ⏳ 待实现：积分系统、订单系统、视频生成服务
